{
  "name": "tessera",
  "version": "0.1.0",
  "private": true,
  "description": "Borderless tiling-terminal workspace (Rust + GTK4). These npm scripts are thin wrappers around cargo for convenience — no Node dependencies.",
  "scripts": {
    "start": "cargo run --release --",
    "dev": "cargo run --",
    "build": "cargo build --release",
    "test": "cargo test",
    "install-app": "bash packaging/install.sh"
  }
}
