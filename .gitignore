/target
/dist
**/*.rs.bk

# Stray desktop screenshots
Screenshot_*.png

# Local data file (real business data — never commit)
/apollo-accounts-export.csv

# npm wrapper (scripts only; no deps)
node_modules/

# Logo experiments (not part of the product)
/logos/
/packaging/tcode_lbl.png
generate_tcode_ai_logos.py

# Working notes & scratch markdown at the repo root — keep only the real docs
/*.md
!/README.md
!/CLAUDE.md

# Subagent-driven-development scratch
/.superpowers/
